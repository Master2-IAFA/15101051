# MLoDS.exe

## Dependencies

- The only dependency in Polyscope, who's included in the repository as submodule.


## Installation
```bash
$ git clone --recursive 
$ cd 150101051
$ mkdir build && cd build
$ cmake .. 
$ make install
```
